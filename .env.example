# .env.example - Variables de entorno para CronoStudio
#
# USO:
#   1. cp .env.example infra/docker/.env
#   2. Edita infra/docker/.env con tus valores
#   3. NUNCA commitees infra/docker/.env a git
#   4. Este archivo SÍ se commitea como documentación
#
# ⚠️ IMPORTANTE: No incluir secretos reales aquí (solo placeholders)

# ============================================================================
# PostgreSQL (Base de datos)
# ============================================================================

POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme123!
POSTGRES_DB=cronostudio
POSTGRES_HOST=cronostudio-postgres
POSTGRES_PORT=5432

# ============================================================================
# n8n (Automatización)
# ============================================================================

N8N_PORT=5678
N8N_WEBHOOK_URL=http://localhost:5678
N8N_ENCRYPTION_KEY=a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6
N8N_USER_MANAGEMENT_DISABLED=false

# ============================================================================
# Next.js Frontend
# ============================================================================

NEXT_PUBLIC_API_URL=http://localhost:3000/api
API_URL=http://localhost:3000/api

# ============================================================================
# Base de Datos (desde Node.js)
# ============================================================================

DATABASE_URL=postgresql://postgres:changeme123!@cronostudio-postgres:5432/cronostudio

# ============================================================================
# Authentication & Security
# ============================================================================

# JWT secret for signing tokens
# Generar: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_key_here_min_32_chars

# Session configuration
SESSION_SECRET=your_session_secret_key_here

# CORS whitelist (comma-separated)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# ============================================================================
# YouTube Integration (Futura)
# ============================================================================

YOUTUBE_API_KEY=change_me
YOUTUBE_CLIENT_ID=change_me
YOUTUBE_CLIENT_SECRET=change_me

# ============================================================================
# General
# ============================================================================

NODE_ENV=development
LOG_LEVEL=info

# ============================================================================
# INSTRUCCIONES
# ============================================================================
#
# 1. POSTGRES_PASSWORD
#    Generar: openssl rand -base64 12
#    Mínimo 12 caracteres con especiales
#
# 2. N8N_ENCRYPTION_KEY
#    CRÍTICO: Generar UNA sola vez con: openssl rand -hex 32
#    Si se pierde, perderás acceso a credenciales en n8n
#    Copiar output completo (64 caracteres hex)
#
# 3. UBICACIÓN CORRECTA
#    Este archivo: .env.example (en raíz, se commitea)
#    Uso: cp .env.example infra/docker/.env (NO se commitea)
#
# 4. VERIFICAR
#    git check-ignore -v infra/docker/.env
#    Debe mostrar: .gitignore:XX:infra/docker/.env
#
# ============================================================================
