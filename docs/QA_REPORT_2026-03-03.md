# QA REPORT - 2026-03-03

## Contexto
- Entorno: local (`http://localhost:3000`) con `RATE_LIMIT_DISABLE=true`.
- Usuario QA temporal: `qa+1772572017@cronostudio.test` (email verificado via API).
- Tests automáticos: `e2e/auth.spec.ts` y `e2e/core-flow.spec.ts`.

## Resultado general
- Estado: **Parcial OK** (flujo core automatizado OK; validaciones manuales externas pendientes).

## Ejecutado (OK)
- Autenticación:
  - Login automático OK (E2E).
  - Verificación de email OK (`/api/auth/verify-email`).
- Ideas:
  - Crear idea OK (E2E) con validaciones activas.
- Scripts:
  - Crear guion OK (E2E).
- Miniaturas:
  - Crear y eliminar miniatura OK (E2E con confirm nativa).
- Producciones:
  - Crear producción OK (E2E).
- Observabilidad:
  - `/api/health` OK (respuesta `status=healthy`).

## Pendiente de verificación manual
- Registro via UI `/register` + email real (n8n).
- Reset de contraseña (`/forgot-password` + `/reset-password`).
- Configuración de cuenta (`/configuracion`): cambio de nombre, email, password, delete account.
- Dashboard: métricas + toasts + sin errores en consola.
- Ideas: editar estado y aprobar con checklist completo (promesa/bullets/hook).
- Producciones: cambio de etapa, fechas, prioridad, borrado.
- Scripts: pipeline conectado n8n y registro en `/api/automation-runs`.
- SEO: CRUD completo.
- Canales: alta y validación en Analytics/Videos.
- Analytics: filtros, subpáginas, gráficos sin errores.
- Automatizaciones n8n: success + error handling.
- Adminer/DB: validación de tablas y conteos.
- CORS/RBAC: pruebas de origen y roles.

## Riesgos / Observaciones
- Rate-limit bloquea QA automatizado si no se desactiva en local.
- Validación de ideas requiere formato estricto (promesa/bullets/hook); UX podría mejorar con plantillas prellenadas.
- Toggle de tema fue removido (modo oscuro fijo). El checklist debe actualizarse en `docs/QA_CHECKLIST.md`.

## Recomendación
- Completar el checklist manual en staging con integraciones reales (n8n, email, analytics).
