# CronoStudio - Session 2026-03-02

## Contexto general
Hoy se cerro el bloque P0 (hardening) y se implemento AI Studio manual-first (Perfiles AI internos). El repo quedo con commits limpios por capa, migracion aplicada en Postgres local y UI nueva en `/ai`.

## Estado actual (resumen corto)
- AI Studio manual-first implementado end-to-end: perfiles, runs, endpoints, UI, tests.
- Migracion `ai_runs` aplicada en Postgres local (docker `cronostudio-postgres`).
- Tests y build green.
- Branch `develop` ahead 10 commits (no push aun).
- Verificacion real de endpoints: C1 OK, C2 pendiente (salida de POST /api/ai/runs no pegada).

## Cambios implementados hoy

### P0.1 hardening auth/service user (cerrado)
- Role invalid/missing => 401; nunca default a owner.
- Webhook exige service user explicito; si falta => 503 `service_user_not_configured`.
- Documentado en `docs/SETUP.md`.
- Tests agregados para role y webhook.

### P0.2 validacion de query params + headers (cerrado)
- Helper `getInt/getString` y validacion de query params.
- `withSecurityHeaders` en todas las respuestas de errores en routes target.
- Tests de invalid query + headers.

### P0.3 AuthContext strict (cerrado)
- Auth estado tri-state y solo valido por sesion real.
- `localStorage` no habilita UI protegida.
- Tests RTL para `AuthContext`.

### AI Studio manual-first (implementado)
**Perfiles internos v1**
- `evergreen_ideas`
- `script_architect`
- `retention_editor`
- `titles_thumbs`

**Persistencia**
- Migracion `infra/migrations/202603020000__ai_runs.sql`.
- Tabla `ai_runs` con `input_json`, `output_json`, status y timestamps.

**Endpoints**
- `GET /api/ai/profiles`
- `POST /api/ai/runs`
- `POST /api/ai/runs/[id]/submit`
- `POST /api/ai/runs/[id]/apply`
- `GET /api/ai/runs?channelId=&limit=&offset=`

**UI**
- Nueva pagina `apps/web/src/app/ai/page.tsx`.
- Selector de canal, cards de perfiles, prompt panel, input JSON, apply, historial por canal.
- Link agregado en nav (`apps/web/src/app/content/navigation.ts`).

**Tests**
- `apps/web/src/__tests__/api/ai-runs.test.ts`
- `apps/web/src/__tests__/api/ai-runs-apply.test.ts`

## Migracion aplicada (docker)
Se ejecuto:
```
docker exec -i cronostudio-postgres sh -lc 'psql -U cronostudio -d cronostudio' < infra/migrations/202603020000__ai_runs.sql
docker exec cronostudio-postgres sh -lc 'psql -U cronostudio -d cronostudio -c "\\dt ai_runs"'
```
Resultado: `ai_runs` creada OK.

## Verificacion real de endpoints (local)
- C1 `GET /api/ai/profiles` OK (mostro 4 perfiles).
- C2 `POST /api/ai/runs` pendiente: falta pegar output del `fetch` ejecutado en la consola del browser.
- C3 `POST /api/ai/runs/execute` OK (OpenAI auto-first):
  - runId: `aa5a7284-0ad5-4079-88cc-4945a9ffd595`
  - perfil: `evergreen_ideas`
  - channelId: `8627fd94-d968-42ac-942b-0e36177fdeb9`
  - applied: 10 ideas creadas (IDs en respuesta)
  - output: 10 ideas evergreen con angle/hook/targetLengthSec
- C4 UI “Generar y aplicar” OK (AI Studio):
  - output valido (10 ideas con title/angle/hook/targetLengthSec)
  - aplicado en DB (ideas creadas)

## Tests y build (ultimo run)
- `npm run test:run` OK (28 files, 92 tests).
- `npm run build` OK.

## Commits creados hoy (AI Studio)
- `db: add ai_runs table`
- `feat: ai profiles + runs api`
- `feat: ai studio ui`
- `test: ai studio runs`

## Lo que queda pendiente (prioridad alta)
1) Completar verificacion real de POST /api/ai/runs (pegar output JSON).
2) Opcional: probar submit/apply manual en UI para validar flujo completo:
   - Generar prompt
   - Pegar JSON valido
   - Validar y guardar
   - Aplicar y ver cambios en ideas/scripts/seo
3) Push a remote si se requiere.

## Notas importantes
- AI Studio es manual-first: no integra servicios externos; el usuario pega JSON.
- `localStorage` ya no habilita auth.
- Webhooks requieren `CRONOSTUDIO_SERVICE_USER_ID` o `CRONOSTUDIO_SERVICE_USER_EMAIL`.

## Siguientes pasos sugeridos (para manana)
1) Ejecutar C2 (POST /api/ai/runs) y guardar respuesta en este doc.
2) Probar UI AI Studio con un canal real:
   - Evergreen ideas -> aplicar -> confirmar nuevas ideas en `/ideas`.
   - Script architect -> aplicar -> confirmar script en `/scripts` y idea en `in_production`.
   - Titles + thumbs -> aplicar -> validar SEO en `seo_data`.
3) Si todo ok, hacer `git push` y abrir PR/merge a `develop`.
